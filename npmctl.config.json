{
  "schemaVersion": 1,
  "site": "peagen.com",
  "repository": "swarmauri/peagen-com",
  "orchestrator": "npmctl-pypi",
  "dns": {
    "provider": "namecheap",
    "zone": "peagen.com",
    "records": [
      {
        "type": "A",
        "host": "@",
        "value": "SITE_SERVER_IP_SECRET",
        "ttl": 300
      },
      {
        "type": "A",
        "host": "*.peagen.com",
        "value": "SITE_SERVER_IP_SECRET",
        "ttl": 300
      },
      {
        "type": "CNAME",
        "host": "www",
        "value": "peagen.com",
        "ttl": 300
      }
    ],
    "providerPackage": "npmctl-namecheap"
  },
  "deploy": {
    "target": "self-hosted-docker",
    "service": "peagen-com",
    "composeFile": "docker-compose.yaml",
    "proxyManager": {
      "provider": "nginx-proxy-manager",
      "apiEndpoint": "NPM_BASE_URL_SECRET"
    }
  },
  "commands": {
    "install": "npm ci",
    "check": "npm run check",
    "build": "npm run build",
    "docker:build": "docker compose build peagen-com",
    "dns:plan": "npmctl plan desired-state/dns.yaml --owner peagen-com",
    "deploy:dry-run": "node scripts/deploy.mjs --dry-run",
    "deploy": "docker compose up -d --build --force-recreate --no-deps peagen-com",
    "npmctl:install": "python -m pip install --upgrade npmctl npmctl-namecheap",
    "npmctl:validate": "npmctl validate desired-state",
    "npmctl:schema": "npmctl schema check",
    "dns:doctor": "npmctl dns doctor --provider namecheap",
    "dns:apply": "npmctl apply desired-state/dns.yaml --owner peagen-com",
    "proxy:validate": "npmctl validate desired-state/proxy.yaml",
    "proxy:plan": "npmctl plan desired-state/proxy.yaml --owner peagen-com",
    "proxy:apply": "npmctl apply desired-state/proxy.yaml --owner peagen-com"
  },
  "npmctl": {
    "package": "npmctl",
    "dnsProviderPackage": "npmctl-namecheap",
    "installCommand": "python -m pip install --upgrade npmctl npmctl-namecheap",
    "proxyManagerApiEndpoint": "NPM_BASE_URL_SECRET"
  }
}
